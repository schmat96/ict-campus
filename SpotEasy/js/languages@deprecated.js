/**
 * JQUERY:
 * @deprecated Diese Methoden werden nicht mehr gebraucht.
 *
 */

/*

var initializeLanguages = function () {
	$(".languages").removeAttr("href");
	$(".languages").click(function() {
		keepInputsSaved(this);
	});
}

function saveInputs(url) {
	$.ajax({
		type: "POST",
		url: url,
		data: {
			email: "test",
			password1: "test2"
		}

	});
}

function callback(url) {
	console.log(url);
}

*/